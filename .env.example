# ======================================================================
# NutriScan · Frontend — variables de entorno
# Copia este archivo como .env y ajusta la URL del backend si hace falta.
# ======================================================================

# URL base de la API del backend.
# Si se omite, usa http://localhost:8080/api por defecto.
VITE_API_URL=http://localhost:8080/api
